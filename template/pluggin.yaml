name: "{{.Name}}"
version: "1.0.0"
author: "{{.Author}}"
description: "{{.Description}}"
entry: "main.lua"
permissions:
  - onebot11
  # - file   # 插件目录内文本文件读写（可选，见 SDK jn.file）
system: false
enabled: true
