# Redrock 插件全集文档

> 最后更新: quiz 改用 SQLite 持久化 + `/答` 命令 + 4 次答错上限；部门链接改为腾讯文档

## 一、插件总览

| 插件 | 类型 | 权限 | 功能 | 核心触发 |
|------|------|------|------|---------|
| `redrock_welcome` | `on_notice` | `onebot11` | 入群欢迎 | 新人进群自动触发 |
| `redrock_faq` | `on_message` + 命令 | `onebot11` | 关键词问答 + 语料库 | @卷娘 + 关键词 |
| `redrock_special` | `on_message` | `onebot11` | 卷娘彩蛋 | @卷娘 + 特定关键词 |
| `redrock_caidanci` | `on_message` + 命令 | `onebot11` | 猜单词游戏 | `/猜单词` 开局，`/猜 <词>` 猜测 |
| `redrock_quiz` | 命令 | `onebot11` + `database` | 红岩快问快答 | `/来一局` 开局，`/答 <答案>` 答题 |
| `redrock_code` | `on_message` + 命令 | `onebot11` + `http` | 在线代码运行 | `/code <语言>` + 换行代码 |
| `redrock_poke` | `on_notice` | `onebot11` | 戳一戳回复 | 戳卷娘 |
| `redrock_group_manager` | `on_message` + `on_notice` + 命令 | `onebot11` + `database` | 群管理工具 | 自动检测 + `/groupstats` |
| `redrock_cron_msg` | `on_timer_call` | `onebot11` | 定时消息 | CronJob JSON 配置触发 |

---

## 二、插件详情

### 2.1 redrock_welcome — 入群欢迎

**触发条件**: `on_notice` 监听 `group_increase` 事件

**行为**: 新成员加入群聊时自动发送 @ 欢迎 + welcome.png 海报。

**回复格式**:
```
@xxx 欢迎到来！这里是红岩网校工作站的新生群，有什么问题都可以问我哦！
没有头绪就试试发送/redrock吧。
[welcome.png]
```

**技术细节**: 
- 事件类型: `notice_type == "group_increase"`
- 消息格式: 富文本段 `{at, text, image}`
- 与 faq 的 `/redrock` 命令形成引导闭环：欢迎 → `/redrock` 菜单 → 数字问答

---

### 2.2 redrock_faq — 关键词问答 + 语料库

**触发条件**: 群聊需 @卷娘，私聊直接触发

**匹配策略**:

| 优先级 | 规则 | 适用场景 |
|--------|------|---------|
| 1 | 语料库 ≤2字符: 清洗后精确匹配 | 短词防误触（6、牛、困、好累） |
| 2 | 语料库 >2字符: 子串匹配 | 日常闲聊（emo、吃饭、摆烂） |
| 3 | 部门问答: 清洗后精确匹配 | 精确提问 |
| 4 | 部门问答: 关键词 + 红岩锚点 | 长句含关键词 |

**锚点词**: `红岩 / 网校 / redrock / 工作站 / 招新`（缺少时仅精确匹配生效）

**部门问答** (17个知识条目，均附带腾讯文档 Q&A 链接):

| 关键词 | 回复 |
|--------|------|
| `红岩网校` / `redrock` | 红岩网校总体介绍 🔗 |
| `产品` / `产品策划` | 产品策划及运营部介绍 🔗 |
| `视觉` / `视觉设计部` | 视觉设计部介绍 🔗 |
| `前端` / `前端研发部` | 前端研发部介绍 🔗 |
| `后端` / `后端研发部` | 后端研发部介绍 🔗 |
| `移动` / `移动开发部` | 移动开发部介绍 🔗 |
| `运维` / `运维安全部` / `运维安全` / `安全` / `sre` / `SRE` | 运维安全部介绍 🔗 |
| `ai` / `人工智能` / `人工智能开发与应用部` / `及应用部` | AI开发部介绍 🔗 |
| `android` / `安卓` | Android子部门介绍 🔗(移动) |
| `ios` | iOS子部门介绍 🔗(移动) |
| `加入` / `报名` / `怎么进` 等 | 加入方式 + join.png 二维码 |
| `在哪` / `地址` / `位置` / `太极运动场` 等 | 太极运动场指引 |
| `能学到什么` / `收获` / `学习` | 学习收获介绍 🔗 |
| `招新` / `宣讲会` | 招新活动介绍 🔗 |
| `成果` / `成就` | 红岩网校成果展示 |
| `新生导航` / `导航` | GIS校园地图 URL |
| `趣味功能` / `功能` | 游戏清单（猜单词/快问快答/运行代码） |

**卷娘语料库** (19条日常闲聊):

| 关键词 | 回复 |
|--------|------|
| `6`★ / `牛`★ / `绝了`★ | 6，卷娘给你扣个666，你也很会冲浪嘛〜 |
| `在吗`★ / `在不在` | 在的在的，卷娘24小时在线（除了睡觉和偷偷摸鱼的时候） |
| `有对象` / `单身` / `有男朋友` / `有女朋友` | 有啊，红岩网校就是我的对象，你要来认识一下吗？ |
| `内卷`★ | 别卷了别卷了，在红岩你可以慢慢学，没人逼你〜 |
| `摆烂`★ / `躺平`★ | 摆烂可以，但也要有技术含量——来运维部学优雅躺平（bushi） |
| `emo` / `难过`★ / `呜呜`★ | 别emo啦！卷娘请你云吃中心食堂杂酱面〜 |
| `吃饭`★ / `吃什么` / `食堂`★ | 推荐中心食堂杂酱面！滨湖也行，别去三食堂（嘘） |
| `早八`★ / `困`★ / `好困`★ / `好累`★ / `累死了` | 早八人早八魂，早八都是人上人 |
| `学长学姐` / `大佬`★ / `大神`★ | 别叫大佬，叫redrocker！大家都从0开始〜 |
| `重邮`★ / `学校`★ | 重邮很好，有红岩网校的重邮更有意思！ |
| `感谢`★ / `谢谢`★ | 不客气！卷娘一直在〜 |
| `拜拜`★ / `再见`★ / `晚安`★ | 拜拜〜记得常来看卷娘哦，不然我会孤单的💔 |
| `校花`★ | cqupt的校花呀，正是卷卷呀，嘿嘿 |
| `学不懂` / `学不会` / `太难了` | 心急则味散，卷卷陪你慢慢来 |
| `无聊`★ | 来杯好茶摇一摇，摇一摇 |
| `特色`★ | 重邮三宝：3G芯片/早操工程/校园跑 |
| `3g` / `3G` | 通芯一号芯片完整科普（长篇） |
| `干嘛`★ / `干啥`★ / `在干嘛` / `你在干嘛` | 卷娘今天也在努力回答邮子们的问题呢！给自己加鸡腿~ |
| `摸鱼`★ / `在摸鱼` | 嘘！+ xu.png 表情 |

> ★ = ≤2字符，使用精确匹配（需 @卷娘 + 清洗后恰好等于关键词）

**数字菜单** (群聊 & 私聊):

| 数字 | 群聊（需@卷娘） | 私聊 |
|------|---------------|------|
| `1` | 红岩网校介绍 | 同 |
| `2` | 七个部门列表 | 同 |
| `3` | 招新活动 | 同 |
| `4` | 常见问题清单 | 同 |
| `5` | 趣味功能 | 同 |

**命令**:

| 命令 | 功能 |
|------|------|
| `/redrock` | 显示完整提问清单 + 快捷菜单（含数字引导） |

**技术细节**:
- 英文关键词 (ai/sre/android/ios): 独立单词边界匹配，避免 `ai` 命中 `email`
- 私聊: 首次发引导菜单，后续走宽松关键词匹配（无锚点要求）
- 后缀剥离: 反复剥离 `SUFFIXES` 列表中的语气词/标点

---

### 2.3 redrock_special — 卷娘彩蛋

**触发条件**: @卷娘 + 关键词（宽松子串匹配）

| 关键词 | 回复 | 图片 |
|--------|------|------|
| `年龄` / `年纪` / `芳龄` / `多大了` | 有些事情你还是不要知道的好 | `img/q1/` 随机2选1 |
| `身高` | 不会吧，不会吧，不会真的有人要问女生的身高吧。 | `img/q2/` 随机2选1 |
| `体重` | emmmm你要不问问别的？ | `img/q3/` 随机2选1 |
| `喜欢什么颜色` / `什么颜色` / `喜欢啥颜色` | 当然是红色！毕竟我是红岩网校的卷娘～ | — |
| `周末干嘛` / `周末干什么` / `周末做什么` | 帮管理员整理文件，偷偷玩猜单词和快问快答 —— 悄悄说我胜率 80% 哦！ | — |

**技术细节**:
- 图片: `math.random(#images)` 随机选取
- emo 关键词已移至 faq 统一管理（避免两个插件对同一关键词给出不同回复）

---

### 2.4 redrock_caidanci — 猜单词游戏

**触发条件**: 命令触发 + 引导词推荐

| 命令 | 功能 |
|------|------|
| `/猜单词 [-l 4/5/6]` | 开局（默认5字母，同群共享） |
| `/猜 <单词>` | 提交猜测 |
| `/提示` | 获取线索（最多4次: 类别→首字母→尾字母→详细） |
| `/结束` | 认输看答案 |

**规则**: 6次机会，🟩=位置正确，🟨=字母存在，⬜=不存在

**引导触发词**: `没意思` / `没劲` / `有啥好玩` / `玩什么` → 推荐玩游戏

> ⚠️ `无聊` 触发词已移至 faq，避免与 faq "来杯好茶摇一摇" 冲突

**单词库**: 62个单词，分技术/校园/通用三类，含红岩相关提示

**技术细节**:
- 状态: 模块级 Lua 表，按群隔离
- 匹配: Wordle 规则（绿色→黄色→灰色三轮扫描）
- 英语猜词: 仅 `/猜 <word>` 命令触发，裸发单词不响应

---

### 2.5 redrock_quiz — 红岩知识快问快答

**触发条件**: 命令触发

| 命令 | 功能 |
|------|------|
| `/来一局` / `/快问快答` | 开局（随机抽5题） |
| `/答 <答案>` | 提交答案 |
| `/结束快答` | 中途退出（显示得分） |

**规则**:
- 答对夸夸 + 下一题
- 答错给提示，**最多答错 4 次**，用完公布正确答案并结束
- 题目可重复回答直到答对（答错只扣次数不跳题）

**评分**:
- 🏆 100%: 满分通关
- 🎯 ≥80%: 红岩知识达人
- 👍 ≥50%: 不错哦
- 📖 <50%: 附腾讯文档 Q&A 链接

**题库**: 8道红岩知识题，支持四种匹配策略:
- 精确匹配: "是"/"2000年10月"
- 全词匹配: 七个部门名必须全部出现
- 最少N匹配: 核心产品至少命中2个
- 任意匹配: 关键词命中一个即算对

**技术细节**:
- 权限: `onebot11` + `database`
- 存储: 内存缓存 + PostgreSQL KV 表（`INSERT ... ON CONFLICT`），重启不丢
- 状态按 `群:用户` 隔离
- 裸发文字不响应，答案必须走 `/答` 命令

---

### 2.6 redrock_code — 在线代码运行

**触发条件**: `/code <语言> [输入参数]` + 换行后的代码块

**示例**:
```
/code py Alice Bob
a = input()
b = input()
print("Hello " + a + " & " + b)
```

**支持语言**: 43种（Judge0 CE language_id 映射表）

**技术细节**:
- 后端: Judge0 `/submissions?wait=true` 同步模式
- Judge0 URL: 配置在 `JUDGE0_BASE_URL` 变量
- 输入: 空格自动替换为 `\n`
- 输出: 自动截断 1500 字符
- 格式: `🔧 Python 3 | 0.01s 3100KB\n\n[输出]`
- 用法提示: 无参数或仅指定语言时附带 `example.png`

---

### 2.7 redrock_poke — 戳一戳回复

**触发条件**: `on_notice` 监听 `notify/poke` 事件

**回复库**: 18条，分四类，防连续重复
- 俏皮傲娇 (5条): 狮子座炸毛、18岁娇贵、爱干净等
- 红岩招新引导 (4条): 引导 `/redrock` / @卷娘 提问
- 游戏功能引导 (4条): 猜单词 → 快问快答 → 运行代码 → Python玩笑
- 人设介绍 + 红岩 (5条): 重庆南岸区、7月31生日、座右铭等

**技术细节**:
- `last_index` 缓存，避免连续两次相同回复
- 回复格式: `[CQ:at,qq=xxx] + 文本`

---

### 2.8 redrock_group_manager — 群管理工具

**五大检测模块** (按优先级):

| 优先级 | 模块 | 触发条件 | 行为 | 管理员通知 |
|--------|------|---------|------|----------|
| 1 | 政治敏感 (28词) | 消息命中 | 撤回 + `@小鬼别碰这话题` | — |
| 2 | 广告 (15词) | 消息命中 | 撤回 + `@交200宣传费` | ✅ DM |
| 3 | 色情 (15词) | 消息命中 | 撤回 | ✅ DM |
| 4 | 图片刷屏 | 2s内≥3张 | 警告 + shuaping.png → 再犯禁言60s | ✅ (禁言时) |
| 5 | +1复读 | 3人同消息 | shuaping_2.png (不@) | — |

**管理员豁免**: `event.admins` 数组中的 QQ 号不受敏感词检测限制

**统计命令**: `/groupstats` (管理员) 查看今日入群/警告/禁言/复读/政治/广告/色情撤回次数

**技术细节**:
- 存储: 内存缓存 + PostgreSQL KV 表（`INSERT ... ON CONFLICT`）
- 图片检测: 匹配 `[CQ:image` / `[CQ:face` / `[CQ:mface` / `[图片]` / `[表情]`
- 消息撤回: `jn.onebot11.delete_msg(message_id)`

---

### 2.9 redrock_cron_msg — 定时消息

**触发条件**: CronJob 定时触发 `on_timer_call`

**Payload 格式**:
```json
{
  "groups": [123456789, 987654321],
  "message": "今天晚上12点卷娘可要清理掉不按规矩改名的同学了哦！再提醒一遍，格式是年级-专业-姓名哦！"
}
```

**技术细节**:
- 支持多群并发发送
- 消息内容完全由 JSON 配置，无需改代码

#### 📌 如何配置 CronJob 发定时消息

1. **打包上传**: `make pack NAME=redrock_cron_msg`，将 `dist/redrock_cron_msg.zip` 上传到 JuanNiang-Neo 的插件市场/`data/pluggins/` 并启用

2. **进入定时任务页面**: Web 面板 → 定时任务 → 新建任务

3. **填写配置**:

   | 字段 | 值 |
   |------|-----|
   | 名称 | 例如「改名提醒」 |
   | Cron 表达式 | 见下方参考 |
   | 目标插件 | 选择 `redrock_cron_msg` |
   | Payload (JSON) | 见下方示例 |

4. **Payload 示例**（换成真实群号）:
   ```json
   {
     "groups": [123456789, 987654321],
     "message": "今天晚上12点卷娘可要清理掉不按规矩改名的同学了哦！再提醒一遍，格式是年级-专业-姓名哦！"
   }
   ```

5. **Cron 表达式参考**:

   | 表达式 | 含义 |
   |--------|------|
   | `0 0 20 * * ?` | 每晚 20:00 |
   | `0 0 21 * * ?` | 每晚 21:00 |
   | `0 0 12 * * ?` | 每天中午 12:00 |
   | `0 30 18 * * ?` | 每天 18:30 |
   | `0 */2 * * * ?` | 每 2 小时一次 |

6. **多群多内容**: 想发不同内容到不同群，新建多个定时任务，每个任务配置不同的 `groups` + `message`

---

## 三、语料库交叉引用

| 关键词 | 归属插件 | 说明 |
|--------|---------|------|
| `无聊` | faq | "来杯好茶摇一摇"，faq 独有，caidanci 已移除 |
| `emo` | faq | "请你吃杂酱面"，faq 统一管理，special 已移除 |
| `卷` | — | 已移除，仅保留 `内卷` |
| `年龄/身高/体重` | special | 彩蛋问答 + 随机图片 |
| `戳一戳` | poke + faq + special | 多渠道互相引用但不冲突 |
| `猜单词` | caidanci + poke + faq + special | 四插件统一引用 `/猜单词` |
| `快问快答` | quiz + poke + faq + special | 四插件统一引用 `/来一局` |
| `运行代码` | code + poke + faq | 三插件统一引用 `/code` |

---

## 四、匹配策略总结

| 策略 | 适用插件 | 规则 |
|------|---------|------|
| 精确匹配 (≤2字符) | faq 语料库 | 清洗后恰好等于关键词，避免短词误触 |
| 宽松子串 | faq 语料库 / special | 消息中出现即命中 |
| 红岩锚点 | faq 部门问答 | 关键词 + 红岩/网校/redrock/工作站/招新 |
| 命令系统 | caidanci/quiz/code/faq | `/xxx` 前缀，优先级高于 on_message |
| 独立词边界 | faq (英文) | ai/sre/android 英文按单词边界匹配 |

---

## 五、加载顺序建议

1. `redrock_welcome` — 入群欢迎（`on_notice`，不阻塞 message）
2. `redrock_group_manager` — 群管理（安全兜底，率先拦截违规消息）
3. `redrock_faq` — 核心问答（群/私消息主处理，最高命中率）
4. `redrock_special` — 彩蛋（faq 后处理未命中的特殊关键词）
5. `redrock_caidanci` — 猜单词（命令为主，引导词在 faq 之后）
6. `redrock_quiz` — 快问快答（命令为主）
7. `redrock_code` — 代码运行（命令为主）
8. `redrock_poke` — 戳一戳（`on_notice`，不阻塞 message）
9. `redrock_cron_msg` — 定时消息（`on_timer_call`，独立触发）
