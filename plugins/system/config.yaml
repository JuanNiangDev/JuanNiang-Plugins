# ====================================================================
# system 插件配置
# ====================================================================
# 系统插件无用户可配置项（配置由主程序内置管理）。
# 插件通过 jn.config.get("key") / jn.config.all() 读取。
# ====================================================================

configs: []